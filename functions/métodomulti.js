const Calculadora = {
    multiplicar: function(a, b) {
      return a * b;
    }
  };
  
  // Ejemplo de uso:
  const resultado = Calculadora.multiplicar(5, 10);
  console.log(resultado);
  
  